import {useEffect,useState} from "react";import {useAppContext} from "../../context/AppContext";import toast from "react-hot-toast";
export default function OwnerBookings(){const {axios,authHeaders,currency,user}=useAppContext();const [data,setData]=useState(null);useEffect(()=>{if(user)axios.get("/api/bookings/hotel",authHeaders()).then(r=>setData(r.data.dashboardData)).catch(e=>toast.error(e.response?.data?.message||e.message))},[user]);return <div><h1 className="text-3xl font-bold">Bookings</h1><p className="mt-1 text-gray-500">Bookings for your hotel.</p><div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="w-full min-w-[800px] text-left text-sm"><thead className="bg-gray-50 text-gray-500"><tr>{["Guest","Room","Check-in","Check-out","Guests","Amount","Payment","Status"].map(x=><th key={x} className="px-4 py-3">{x}</th>)}</tr></thead><tbody>{data?.bookings?.map(b=><tr key={b._id} className="border-t"><td className="px-4 py-3">{b.user?.username}</td><td className="px-4 py-3">{b.room?.roomType}</td><td className="px-4 py-3">{new Date(b.checkInDate).toLocaleDateString()}</td><td className="px-4 py-3">{new Date(b.checkOutDate).toLocaleDateString()}</td><td className="px-4 py-3">{b.guests}</td><td className="px-4 py-3">{currency}{b.totalPrice}</td><td className="px-4 py-3">{b.isPaid?"Paid":"Unpaid"}</td><td className="px-4 py-3 capitalize">{b.status}</td></tr>)}</tbody></table>{!data?.bookings?.length&&<p className="p-10 text-center text-gray-500">No bookings found.</p>}</div></div>}
